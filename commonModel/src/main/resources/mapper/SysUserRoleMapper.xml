<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.customerservice.mapper.SysUserRoleMapper">

    <select
            id="findRoleCodesByUserId"
            resultType="java.lang.String">

        SELECT DISTINCT
            role.role_code
        FROM sys_user_role AS user_role

                 INNER JOIN sys_role AS role
                            ON role.id = user_role.role_id

        WHERE user_role.user_id = #{userId}
          AND role.status = 'ENABLED'

        ORDER BY role.role_code ASC

    </select>

    <select
            id="findAllRoleCodesByUserId"
            resultType="java.lang.String">

        SELECT DISTINCT
            role.role_code
        FROM sys_user_role AS user_role

                 INNER JOIN sys_role AS role
                            ON role.id = user_role.role_id

        WHERE user_role.user_id = #{userId}

        ORDER BY role.role_code ASC

    </select>


    <insert id="assignRole">

        INSERT INTO sys_user_role
        (
            user_id,
            role_id
        )
        VALUES
            (
                #{userId},
                #{roleId}
            )
        ON DUPLICATE KEY UPDATE role_id = VALUES(role_id)

    </insert>


    <delete id="removeRole">

        DELETE FROM sys_user_role
        WHERE user_id = #{userId}
          AND role_id = #{roleId}

    </delete>

</mapper>
