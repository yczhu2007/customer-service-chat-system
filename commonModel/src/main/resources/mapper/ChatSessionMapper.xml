<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.customerservice.mapper.ChatSessionMapper">
    <select id="findActiveByUserId"
            resultType="com.example.customerservice.domain.ChatSession">

        SELECT *
        FROM chat_session
        WHERE user_id = #{userId}
          AND status = 'ACTIVE'
        LIMIT 1

    </select>
    <update id="endSession">

        UPDATE chat_session
        SET
            status = 'CLOSED',
            end_time = #{endTime}
        WHERE id = #{id}
          AND status = 'ACTIVE'

    </update>
</mapper>
